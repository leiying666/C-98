Description
有n人围成一圈，顺序排号。从第1个人开始报数（从1到3报数），凡报到3的人退出圈子，问最后留下的是原来的第几号的那位。
Input
初始人数n
Output
最后一人的初始编号
Sample Input
3
Sample Output
2



#include<stdio.h>
#include<stdlib.h>
int main()
{
    int n;
    scanf("%d", &n);
    int* pa = (int*)malloc(n * 4);
    if (pa == NULL)
    {
        return 0;
    }
    int i,c=0,num=n;
    for (i = 0; i < n; i++)
    {
        pa[i] = 1;
    }
    i = 0;
    while (num!=1)
    {
        c++;
        if (pa[i] == 0)
        {
            c--;
        }
        if (c % 3 == 0&&c!=0)
        {
            c = 0;
            pa[i] = 0;
            num--;
            if (num == 1)
            {
                break;
            }
        }
        i++;
        if (i >= n)
        {
            i = 0;
            while (pa[i] == 0)
            {
                i++;
            }
        }
    }
    while (pa[i] == 0)
    {
        i++;
        if (i >= n)
        {
            i = 0;
        }
    }
    printf("%d", i + 1);
    free(pa);
    pa = NULL;
    return 0;
}
